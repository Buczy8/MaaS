# 1. Uzupelnij wartosci, a nastepnie zapisz jako ansible/secrets.yml.
# 2. Zaszyfruj plik komenda: ansible-vault encrypt ansible/secrets.yml

pg_monitor_password: "CHANGE_ME_TO_STRONG_PASSWORD" # Haslo konta monitorujacego tworzonego w roli mariadb.

